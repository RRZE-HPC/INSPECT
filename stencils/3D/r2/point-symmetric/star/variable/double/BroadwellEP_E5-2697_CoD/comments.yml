spatial_blocking:
  {author: Julian Hammer, comment: 'A little low performance, but still disdinguishable
    from unblocked code.', review: orange}
grid_scaling:
  {author: Julian Hammer, comment: 'Cache thrashing is happening, but hardly shown in
    the measurement. Measurement is > 10% off from prediction. With increasing memory traffic per iteration, the
    discrapency grows.', review: orange}
data_transfers:
  {author: Julian Hammer, comment: 'Cache simulator predicths thrashing effects, which
    only seldom show in measurements.', review: orange}
thread_scaling:
  {author: Julian Hammer, comment: null, review: green}
